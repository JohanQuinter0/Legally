<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/light_gray_fix"/>  <!-- Color de fondo -->
    <corners android:radius="12dp"/>  <!-- Bordes redondeados -->
    <stroke android:width="1dp" android:color="@color/light_gray_fix"/>  <!-- Borde gris -->
</shape>